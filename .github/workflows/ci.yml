name: ci

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  fortifications:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: make fortifications

